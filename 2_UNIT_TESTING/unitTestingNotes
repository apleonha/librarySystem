Unit Testing is performed on the first version of the program, prior to the GUI.
Unit testing is performed usign JUnit, and can be implemented into the java project using the unit test wizard in eclipse, like I did, or with Maven.

Because many of the methods are void, unit testing with JUnit wasn't as successful as simply checking the database output after other tests were run. The minimal code used is included, but the following indicates how testing was performed during development when JUnit tests were not as usable.
